import React from 'react';
import './styles.css';

const SearchForm = () => (
    <form className="SearchForm">
        <input className="searchInput" placeholder="Search for movies by name..." />
    </form>
);

export default SearchForm;
